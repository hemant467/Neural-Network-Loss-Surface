# Neural 🧬 Network Loss Surface

This project visualizes a **non-convex neural network loss surface** defined as:

<div align="center">

L(w₁, w₂) = sin(w₁)cos(w₂) + 0.1(w₁² + w₂²)

</div>

```
L(w1, w2) = sin(w1)cos(w2) + 0.1(w1^2 + w2^2)
```

The MATLAB script generates a 3D surface plot over the weight space (w1​,w2​) illustrating key characteristics such as:

- Multiple local minima
- Saddle points
- Challenges of non-convex optimization

The visualization is enhanced with smooth shading, lighting, and a perceptually uniform colormap for clearer interpretation.

**Purpose 📜**  
To build intuition about loss landscapes encountered in neural network training and optimization.

<img src="https://readme-typing-svg.herokuapp.com/?lines=Neural+🧬Network;Loss+Surface&font=Fira+Code&color=FFD700&center=true&width=420&height=45" alt="Typing SVG" />
